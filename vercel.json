{
  "version": 2,
  "buildCommand": "pnpm --filter @workspace/court-session-management build && node -e \"const fs=require('fs'); const src=fs.existsSync('dist')?'dist':(fs.existsSync('artifacts/court-session-management/dist')?'artifacts/court-session-management/dist':null); if(src && src!=='dist') fs.cpSync(src, 'dist', {recursive: true});\"",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
