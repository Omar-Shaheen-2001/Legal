{
  "version": 2,
  "buildCommand": "pnpm --filter @workspace/court-session-management build && node scripts/copy-dist.js",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
